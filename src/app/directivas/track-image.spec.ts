import { TrackImage } from './track-image';

describe('TrackImage', () => {
  it('should create an instance', () => {
    const directive = new TrackImage();
    expect(directive).toBeTruthy();
  });
});
